{
  "name": "agent-comms-service",
  "version": "0.1.0",
  "description": "POC: Tunnel Broker + BFF proxy. Accepts outbound-only WebSocket connections from data-plane Tunnel Agents and relays HTTP requests to them by tenant/data-plane ID.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
